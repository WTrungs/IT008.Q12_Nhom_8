<Page x:Class="TetrisApp.Views.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TetrisApp.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SettingsPage">

    <Grid Margin="30">
        <StackPanel Width="420">
            <TextBlock Text="Settings" FontSize="32" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

            <CheckBox x:Name="MusicToggle" VerticalAlignment="Center" VerticalContentAlignment="Center" Margin="0,0,0,16">
                <TextBlock Text="Music" VerticalAlignment="Center" FontSize="16"/>
            </CheckBox>

            <TextBlock Text="Volume" FontSize="16"/>
            <Slider x:Name="VolumeSlider" Minimum="0" Maximum="1" TickFrequency="0.01" IsSnapToTickEnabled="True" Margin="0,0,0,16"/>

            <TextBlock Text="Select Track" FontSize="16"/>
            <ComboBox x:Name="TrackCombo" Height="16" Margin="0,0,0,16">
                <ComboBoxItem Content="Track 1"/>
                <ComboBoxItem Content="Track 2"/>
                <ComboBoxItem Content="Track 3"/>
            </ComboBox>

            <Button Content="Save and Back to Menu" Height="36" Margin="0,24,0,0" Click="Back_Click"/>
        </StackPanel>
    </Grid>
</Page>
