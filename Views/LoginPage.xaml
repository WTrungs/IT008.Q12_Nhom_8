<Page x:Class="TetrisApp.Views.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TetrisApp.Views"
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="1000"
      Title="LoginPage"
      Loaded="LoginPage_Loaded"
      PreviewKeyDown="Page_PreviewKeyDown"
      UseLayoutRounding="True"
      SnapsToDevicePixels="True">

    <Page.Resources>
        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#8899AC"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style x:Key="SpaceTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#1A1F2E"/>
            <Setter Property="Foreground" Value="#E6ECFF"/>
            <Setter Property="BorderBrush" Value="#3A4058"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="CaretBrush" Value="#22D3EE"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#22D3EE"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#22D3EE" BlurRadius="10" Opacity="0.4" ShadowDepth="0"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#505A75"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SpacePasswordBox" TargetType="PasswordBox">
            <Setter Property="Background" Value="#1A1F2E"/>
            <Setter Property="Foreground" Value="#E6ECFF"/>
            <Setter Property="BorderBrush" Value="#3A4058"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="CaretBrush" Value="#22D3EE"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#22D3EE"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#22D3EE" BlurRadius="10" Opacity="0.4" ShadowDepth="0"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#505A75"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="{StaticResource SpaceBackground}">
        <Canvas IsHitTestVisible="False">
            <Ellipse Width="800" Height="800" Canvas.Left="-200" Canvas.Top="-200" Fill="#2A0044">
                <Ellipse.Effect>
                    <BlurEffect Radius="150"/>
                </Ellipse.Effect>
            </Ellipse>
            <Ellipse Width="600" Height="600" Canvas.Right="-100" Canvas.Bottom="-100" Fill="#003366">
                <Ellipse.Effect>
                    <BlurEffect Radius="120"/>
                </Ellipse.Effect>
            </Ellipse>
        </Canvas>

        <Border Style="{StaticResource GlassCard}" 
                Width="420" VerticalAlignment="Center" HorizontalAlignment="Center"
                Padding="40">
            <Grid x:Name="RootGrid">
                <StackPanel>
                    <TextBlock x:Name="TitleText" Text="LOGIN" FontSize="32" FontWeight="Bold" 
                               HorizontalAlignment="Center" Margin="0,0,0,30" 
                               Foreground="#E6ECFF"/>

                    <TextBlock Text="USERNAME" Style="{StaticResource LabelText}"/>
                    <TextBox x:Name="UsernameTextBox" Style="{StaticResource SpaceTextBox}"/>

                    <TextBlock Text="PASSWORD" Style="{StaticResource LabelText}" Margin="0,15,0,5"/>
                    <PasswordBox x:Name="PasswordBox" Style="{StaticResource SpacePasswordBox}"/>

                    <Grid Margin="0,5,0,25">
                        <TextBlock x:Name="ForgotPasswordLink" HorizontalAlignment="Right">
                            <Hyperlink Click="ForgotPasswordLink_Click" Foreground="#22D3EE" TextDecorations="None">
                                Forgot Password?
                            </Hyperlink>
                        </TextBlock>

                        <TextBlock x:Name="BackToLoginLink" HorizontalAlignment="Right" Visibility="Collapsed">
                            <Hyperlink Click="BackToLoginLink_Click" Foreground="#FF6B6B" TextDecorations="None">
                                &lt; Back to Login
                            </Hyperlink>
                        </TextBlock>
                    </Grid>

                    <Button x:Name="LoginButton" Style="{StaticResource SpaceButton}" 
                            Content="LOGIN" Height="50" FontSize="16" FontWeight="Bold"
                            Click="LoginButton_Click"/>

                    <StackPanel x:Name="SignUpPanel" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                        <TextBlock Text="New Player? " Foreground="#8899AC"/>
                        <TextBlock>
                            <Hyperlink x:Name="SignUpLink" Click="SignUpLink_Click" Foreground="#22D3EE" TextDecorations="None">
                                Create Account
                            </Hyperlink>
                        </TextBlock>
                    </StackPanel>

                    <Button x:Name="ContinueAsGuestButton" Style="{StaticResource SpaceButton}" 
                            Content="CONTINUE AS GUEST" Height="40" Margin="0,15,0,0" 
                            Background="#33FFFFFF" FontSize="14" Opacity="0.8"
                            Click="ContinueAsGuestButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>